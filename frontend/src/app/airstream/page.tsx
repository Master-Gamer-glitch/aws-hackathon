import AirstreamConsole from "@/components/airstream/AirstreamConsole";

export const metadata = {
  title: "Airstream — Device Rooms",
};

export default function AirstreamPage() {
  return <AirstreamConsole />;
}
