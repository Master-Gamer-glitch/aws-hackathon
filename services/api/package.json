{
  "name": "@crewdesk/api",
  "version": "1.0.0",
  "type": "module",
  "description": "CrewDesk Backend API - Lambda handlers for task distribution and agent execution",
  "main": "index.js",
  "scripts": {
    "test": "node --test handlers/**/*.test.mjs",
    "start": "sam local start-api",
    "build": "sam build",
    "deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.300.0",
    "@aws-sdk/lib-dynamodb": "^3.300.0",
    "@aws-sdk/client-bedrock-runtime": "^3.300.0",
    "@aws-sdk/client-s3": "^3.300.0",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.300.0"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0"
  }
}
